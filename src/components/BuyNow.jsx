import { Link } from "react-router-dom";

export default function NavBar() {
  return (
    <button className="mt-4 border-1 border-white hover:bg-black hover:cursor-pointer text-white px-6 py-2 rounded-sm transition w-full">
      Buy Now
    </button>
  );
}
